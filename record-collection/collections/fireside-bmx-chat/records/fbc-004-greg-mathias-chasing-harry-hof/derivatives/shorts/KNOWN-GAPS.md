# Known Gaps

## GM-010

No record numbered 10 appears in the supplied Studio evidence or working-source PDF. No capture, title, description, URL, date, visibility state, or transcript has been assigned to it.

## Direct Short URLs and video IDs

The supplied published-frame captures and YouTube Studio screenshots do not expose direct Short URLs or video IDs. Those fields remain null. The parent full-length URL and supplied podcast platform URLs are preserved.

## GM-012A working-source entry

The distinct 0:36 record preserved as GM-012A appears in Studio evidence and has its own frame capture, visible title, duration, date, status, and a visible description. That Studio description is preserved. A separate numbered GM-012A title/description entry does not appear in the working-source PDF, which contains only one numbered 12 entry. The inconsistency is documented rather than silently resolved.

## Standalone Short transcripts

No standalone Short transcript files were supplied. Each child record identifies a provisional window in the supplied timestamped parent transcript. Those windows are access references and still require audiovisual verification.

## UI-truncated titles

Several Studio titles end outside the visible column. The visible text is transcribed and an ellipsis marks truncation; missing text is not guessed.

## Parent publication date

The supplied supporting material includes planning language for an August 2025 release, but no verified parent-video publication date was supplied. The parent dossier therefore retains a null publication date. The Short dates are separately documented from Studio evidence.
