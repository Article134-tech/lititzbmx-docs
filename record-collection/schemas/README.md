# Schemas

`record-metadata.schema.json` defines the minimum machine-readable fields for a Lititz BMX dossier. Metadata files may contain additional fields when a record requires them.

Validation is part of the repository build. See `docs/VALIDATION_REPORT.md`.
